# Compatibility-only access bridge for the client-side Powah world-unload cleanup.
# The field remains private and is accessed only through VRO's optional mixin accessor.
public owmii.powah.block.cable.CableNet

# Required by the guarded ModernFix-derived STB atlas stitcher.
public net.minecraft.client.renderer.texture.Stitcher$Holder
